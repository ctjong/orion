{
    "name": "orion-scripts",
    "version": "0.0.0",
    "scripts": {
        "build": "node scripts/build.js",
        "test": "mocha --require source-map-support/register test/client/main.js"
    }
}
